@extends('layout.default')

@section('content')
	<div id="head">
		<div id="left" class="col-sm-9">Logo</div> 
	</div>
	<div id="slideshow">
		<div class="col-sm-12" align="center">
			image resposive new
		</div>
	</div>
	<div id="content"> 
			<div id="content_left" class="col-sm-4">
				content left
			</div>
			<div id="content_midle" class="col-sm-4">
				content midle
			</div>

			<div id="content_right" class="col-sm-4">
				content right
			</div>
	</div>

	<div id="footer">
		<div  class="col-sm-3">
			<h5 align="center">CSS </h5>
				<p>But on medium devices the design may be better as a 50%/50% split.

Tip: Medium devices are defined as having a screen width from 992 pixels to 1199 pixels.

For medium devices we will use the .col-md-* classes.

Now we will add the column widths for medium devices:</p>
			</div>
			<div class="col-sm-3">
				<h5 align="center">CSS </h5>
				<p>But on medium devices the design may be better as a 50%/50% split.

Tip: Medium devices are defined as having a screen width from 992 pixels to 1199 pixels.

For medium devices we will use the .col-md-* classes.

Now we will add the column widths for medium devices:</p>
			</div>
			<div class="col-sm-3">
				<h5 align="center">CSS </h5>
				<p>But on medium devices the design may be better as a 50%/50% split.

Tip: Medium devices are defined as having a screen width from 992 pixels to 1199 pixels.

For medium devices we will use the .col-md-* classes.

Now we will add the column widths for medium devices:</p>
			</div>
			<div class="col-sm-3">
				<h5 align="center">CSS </h5>
				<p>But on medium devices the design may be better as a 50%/50% split.

Tip: Medium devices are defined as having a screen width from 992 pixels to 1199 pixels.

For medium devices we will use the .col-md-* classes.

Now we will add the column widths for medium devices:</p>
			</div>
	</div>

@endsection

